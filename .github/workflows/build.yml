name: build

on:
  push:
    branches: [ci]

jobs:
  build-package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up environment
        uses: actions/setup-python@v4
        with:
          python-version: 3.12
          cache: 'pip'
      - id: install-hatch
        run: pip install hatch
      - id: bump-version
        run: echo "__version__ = `hatch run bump-version`" > ./src/kc3zvd/iot_state/__about__.py
      - id: check-version
        run: cat ./src/kc3zvd/iot_state/__about__.py
    outputs:
      version: ${{ steps.bump-version.outputs.version }}
        