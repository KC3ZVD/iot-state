name: build

on:
  push:
    branches: [ci]

jobs:
  version:
    runs-on: ubuntu-latest
    steps:
      - id: install-hatch
        run: pip install hatch
      - id: bump-version
        run: echo "version=`hatch run ci:bump-version`" >> "$GITHUB_OUTPUT"
    outputs:
      version: ${{ steps.bump-version.outputs.version }}
  verify:
    runs-on: ubuntu-latest
    needs: version
    steps:
      - env:
          NEW_VERSION: ${{ needs.version.outputs.version }}
        run: env
        